#include "game.h"

int main() {
    pf::Game game;
    game.play();

    // sf::RenderWindow window(
    //     sf::VideoMode(100, 100),
    //     "Push Fight"
    // );

    // sf::RenderWindow window = sf::RenderWindow(
    //     sf::VideoMode(100, 100),
    //     "Push Fight"
    // );

    return 0;
}